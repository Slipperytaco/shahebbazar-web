# File reference

Every file in the repo and what it does. Generated by reading the code, not
the docs — where a comment and the behaviour disagreed, the behaviour won.

Files marked **DEAD** are superseded and should not be run or edited.
Files marked **BROKEN** throw against the current schema.

---

## Root

| File | What it does |
| --- | --- |
| `package.json` | No application code. Holds the `concurrently` scripts that run the API and the frontend together (`npm run dev`), plus the `db:load` / `db:reset` / `build` / `lint` / `typecheck` pass-throughs to the sub-projects. |
| `package-lock.json` | Lockfile for that one dev dependency (`concurrently`). |
| `.gitignore` | Applies to the whole tree recursively. Ignores `node_modules/` at any depth, `.env` and `.env.*` (but keeps `.env.example`), `.next`, `*.tsbuildinfo`. |
| `README.md` | Setup, architecture, conventions, troubleshooting table, known gaps. The primary onboarding document. |

---

## backend/ — Express API, port 4000

| File | What it does |
| --- | --- |
| `package.json` | Declares Express 5, `pg`, `cors`, `dotenv`, `multer`. Scripts: `dev` uses `node --watch` for reload on save. **`socket.io` is declared but never imported anywhere** — presumably intended for the messaging feature. |
| `.env.example` | Template for `backend/.env`. Postgres connection vars, `PORT`, `CORS_ORIGIN`. Copy it, then set `PGPASSWORD` — nothing connects until you do. |
| `db.js` | The entire database connection layer, 11 lines. Loads `.env` via dotenv, creates one `pg.Pool`, exports it. Every route file `require`s this same pool — Node caches modules, so there is exactly one pool per process. Connects lazily: creating the pool does not touch Postgres. |
| `index.js` | Server entry point. Registers middleware **in order** (CORS → JSON body parser → static `/uploads` → three routers → `/api/health` → `/`), starts the listener, prints the schema report at startup, and handles `EADDRINUSE` with the `npx kill-port` remedy instead of a stack trace. |
| `lib/verifySchema.js` | Checks that the 14 tables and views the API depends on actually exist, in **one** query, using `to_regclass()` (which returns NULL for a missing object instead of raising). Each name is mapped to the SQL file that defines it, so the error tells you which file to run. Used by `/api/health` and by the startup report. |
| `routes/public.js` | The real API. 478 lines, all read-only, all approved-rows-only. Serves `/api/home`, `/api/categories`, `/api/businesses`, `/api/businesses/:slug`. Contains the two shared SQL constants (`CATEGORY_TILE_SQL`, `BUSINESS_CARD_SQL`), the sort whitelist, the facet queries, and `errorBody()` which hides Postgres messages in production. |
| `routes/vendors.js` | **BROKEN.** `GET /` returns `SELECT * FROM vendors` with no status filter — leaks `vendor_rejection_reason` and moderation state to anonymous callers. `POST /` inserts `vendor_password` and `vendor_city`, columns that no longer exist, and omits the required `user_id` and `vendor_slug`. Written against the old `schema.sql`. |
| `routes/vendorListings.js` | **BROKEN** (the insert). Configures `multer` for photo uploads (generated filenames, JPEG/PNG only). `POST /vendors/:id/listings` inserts `title, description, price, category` — all four renamed in v2. The photo upload, listing fetch and delete routes work. Stores `file.path`, which contains a backslash on Windows and is not a valid URL. |
| `scripts/load-db.js` | The `db:load` / `db:reset` command. Verifies all six SQL files exist, connects to the `postgres` maintenance database to `CREATE`/`DROP DATABASE` (with `pg_terminate_backend` to kick connections that would block a drop), applies the six files in order, prints row counts. Maps failure codes to remedies. |
| `uploads/*.jpg` | Five uploaded images, served by `express.static`. Not gitignored, so they are committed. |

---

## database/

Run order is fixed. `load-db.js` enforces it; if you load by hand in pgAdmin,
follow the numbers.

| # | File | What it does |
| --- | --- | --- |
| 1 | `schema.v2.sql` | Core schema, 459 lines. 18 tables in 9 numbered sections (identity, geography, taxonomy, vendors, listings, quotations, messaging, reviews, analytics) plus 3 admin views. Enables `pg_trgm`. Status columns use `CHECK` rather than `ENUM` so extending them is a one-line migration. |
| 2 | `schema.v2.1.sql` | Additive. Adds `vendor_opening_hours`, `events`, `saved_businesses`, and **`v_business_cards`** — the view behind every business list on the site. |
| 3 | `seed.v2.sql` | B2B sample data: the location hierarchy (Bangladesh → Rajshahi → areas), categories, merchants, listings, RFQs. Inserts by slug lookup rather than hard-coded ids, so it stays correct if partially re-run. |
| 4 | `seed.v2.1.sql` | Consumer directory content for the home page — opening hours, events. |
| 5 | `schema.v2.2.sql` | Additive. Adds `vendors.vendor_website` and the `vendor_facts` table (label/value pairs, so a hospital can record beds and a hotel can record check-in time without a migration per business type). |
| 6 | `seed.v2.2.sql` | Profile detail: services, facts, written reviews. |
| — | `schema.sql` | **DEAD.** The original schema. Superseded by v2. The two broken routes were written against this. |
| — | `sampleinput.sql` | **DEAD.** Original seed, 4 lines. |
| — | `SCHEMA-NOTES.md` | Why v2 differs from v1, mapped to the client's scope email. §4 lists the exact code changes the broken routes need. Read this before arguing with the schema. |

### The four views

| View | Purpose |
| --- | --- |
| `v_business_cards` | Public card projection. Derives primary category, rating, review count and today's open state via `LEFT JOIN LATERAL`, and ends in `WHERE vendor_status = 'approved'` — which is why unapproved shops 404 with no explicit check in the route. `open_state` is three-valued: `open`, `closed`, or NULL meaning *hours unknown*. |
| `v_search_trends_daily` | Popular-search chips and admin trends. `HAVING COUNT(*) >= 5` suppresses rare terms, so one person's query cannot become a "popular search" or re-identify them. |
| `v_active_users_daily` | Distinct session hashes per day. Never counts accounts. |
| `v_moderation_queue` | `UNION ALL` of pending vendors + pending listings + open reports into one admin list. |

---

## docs/

| File | What it does |
| --- | --- |
| `REQUIREMENTS.md` | The client's requirements. |
| `CODEBASE-GUIDE.html` | Full architectural walkthrough — open in a browser. |
| `FILE-REFERENCE.md` | This file. |

---

## frontend/shahebbazar/ — Next.js, port 3000

### Config

| File | What it does |
| --- | --- |
| `package.json` | Next 16, React 19, Tailwind 4, TypeScript 5. Two icon libraries on purpose: `lucide-react` for UI icons, `@icons-pack/react-simple-icons` for brand marks (Lucide removed trademarked logos). |
| `next.config.ts` | One setting: pins the Turbopack root to this folder. Without it Next sees two lockfiles (this one and the repo root's), guesses wrong about the workspace root, and warns on every start. |
| `tsconfig.json` | Sets the `@/*` path alias to this directory — that is what makes `@/lib/api` work. |
| `postcss.config.mjs` | Loads `@tailwindcss/postcss`. Tailwind 4 has no `tailwind.config.js`; configuration lives in the CSS. |
| `eslint.config.mjs` | Next core-web-vitals + TypeScript rules. |
| `next-env.d.ts` | Generated by Next. Do not edit. |
| `.env.example` / `.env.local` | The three frontend variables. `API_BASE_URL` has no `NEXT_PUBLIC_` prefix so it stays server-side; `NEXT_PUBLIC_ASSET_BASE_URL` and `NEXT_PUBLIC_SITE_URL` are exposed to the browser because they end up in `<img src>` and in share cards. |
| `.gitignore` | Redundant with the root one, but harmless. |
| `tsconfig.tsbuildinfo` | TypeScript incremental build cache. Generated. |
| `AGENTS.md` / `CLAUDE.md` | Auto-generated by `next dev` — a note that Next 16 differs from older versions. Not project documentation. `CLAUDE.md` is one line importing `AGENTS.md`. |
| `README.md` | The untouched `create-next-app` boilerplate. The real README is at the repo root. |

### app/ — routes

Folders in parentheses are **route groups**: they organise files without
adding a URL segment, so `app/(public)/page.tsx` serves `/`, not `/public`.

| File | Route | What it does |
| --- | --- | --- |
| `layout.tsx` | all | The root layout. Renders `<html>`, loads the Geist font, sets site-wide metadata and `metadataBase` (which resolves relative OpenGraph image paths to absolute URLs — link previews require that). |
| `globals.css` | — | The entire design system. Tailwind 4 `@theme` block declaring every colour, font, and shadow token; `--color-brand-600` generates `bg-brand-600`, `text-brand-600`, `border-brand-600`. Also holds a `@layer components` shim for the legacy `.form-*` classes on the vendor pages. |
| `favicon.ico` | — | Tab icon. |
| `(public)/layout.tsx` | — | Marks everything below it indexable. Deliberately applies **no** auth gate. Does not render page chrome — that is `AppShell`'s job, because layouts do not receive `searchParams` and the locale is a query parameter. |
| `(public)/page.tsx` | `/` | Home. An `async` server component: awaits `searchParams` and `getHomeData()` together, then composes Hero + CategoryGrid + FeaturedBusinesses in the main column and three rail cards on the right. Emits `WebSite` JSON-LD with a `SearchAction`. |
| `(public)/search/page.tsx` | `/search` | Search results. Reads `q`, `category`, `area`, `sort`, `page` from the URL; fetches results and the cached home payload (for the category dropdown) in parallel. Renders sponsored rows in their own labelled block above the organic ones. Pagination is derived from the returned page size, not from `total`, because `total` includes sponsored rows. `noindex, follow` — arbitrary queries would create unbounded near-duplicate pages. |
| `(public)/business/[slug]/page.tsx` | `/business/<slug>` | Shop profile. `generateMetadata()` builds the title from the shop's real name, category and area and emits per-business OpenGraph tags; the page body renders profile + sidebar and a `LocalBusiness` JSON-LD block. Only populated fields go into the JSON-LD — an `aggregateRating` with zero reviews is invalid structured data and is reported as an error, not ignored. `null` from the API becomes `notFound()`. |
| `(public)/vendors/register/page.jsx` | `/vendors/register` | **Legacy.** A `"use client"` form posting directly to `http://localhost:4000` from the browser. Still sends a password field; the client asked for phone OTP. Its POST target is broken. |
| `(seeker)/layout.tsx` | — | Auth gate for the signed-in customer area. **Currently empty** — a `TODO(auth)` comment and `return children`. Sets `robots: noindex`. |
| `(seeker)/account/page.tsx` | `/account` | Placeholder. Lists the five sections that belong here (saved businesses, quote requests, messages, reviews, settings) and names the table behind each. No approved design yet. |
| `(provider)/layout.tsx` | — | Auth gate for shop owners. **Currently empty.** The comment says explicitly these routes must not deploy in this state. |
| `(provider)/vendors/dashboard/page.jsx` | `/vendors/dashboard` | **Legacy.** Client component. Fetches *every* vendor into a `<select>` and auto-selects the first, because there is no session to say which vendor you are. |
| `(provider)/vendors/dashboard/add-listing/page.jsx` | — | **Legacy.** The add-listing form. Two-step submit: create the listing, read back `listing_id`, then upload photos as `multipart/form-data`. The first step is broken against v2. |
| `(provider)/vendors/dashboard/VendorListings.jsx` | — | **Legacy.** Lists and deletes a vendor's listings. Reads `listing.title` / `.price` / `.category`, which v2 renamed. |
| `(admin)/layout.tsx` | — | Auth gate for admins. **Currently empty.** The comment specifies `notFound()` rather than a redirect, so an unauthorised request is not told the area exists. Documents the admin scope and the four views that support it. |
| `(admin)/admin/page.tsx` | `/admin` | Placeholder. |

### lib/ — the four files that carry the logic

| File | What it does |
| --- | --- |
| `api.ts` | The only file that knows the API exists. `getHomeData()`, `searchBusinesses()`, `getBusiness(slug)`, plus `assetUrl()`. All run server-side. Sets the cache window per endpoint (home and profile 300s, search 60s) via `next: { revalidate }`. Two deliberate failure policies: `getHomeData` swallows errors and returns empty arrays so the page shell still renders; `getBusiness` rethrows anything but a 404, because a half-rendered profile looks complete and is wrong. `assetUrl()` turns a stored relative path into an absolute browser URL and normalises Windows backslashes. |
| `types.ts` | The API contract as TypeScript interfaces, field names matching the database columns exactly. Change a column in Express without changing this and `npm run typecheck` fails — the mismatch surfaces at compile time instead of as blank fields. Documents the `pg` NUMERIC-returns-a-string trap. |
| `i18n.ts` | Every user-facing string in English and Bangla, 331 lines. Plus `resolveLocale()` (reads `?lang=`), `localeHref()` (appends it to links so the language survives navigation), and `pick(locale, en, bn)` which falls back to the other language when a translation is null, so a field is never rendered empty. No hardcoded English in JSX — that is a hard rule. |
| `format.ts` | Display formatting, server-side only. `toNumber` (the NUMERIC-string fix), `formatRating`, `formatClockTime` (SQL `TIME` → "9:30 AM"), event date helpers pinned to `Asia/Dhaka`, and `localiseDigits` which maps ASCII digits to Bengali numerals. Timezone is pinned because formatting the same date on both server and client produces a hydration mismatch. |

### components/shared/ — used by more than one area

| File | What it does |
| --- | --- |
| `SiteHeader.tsx` | Brand, global search, language toggle, log-in and register buttons. The search is a plain `<form action="/search" method="get">` — no JavaScript, and every search is a linkable URL. |
| `SiteFooter.tsx` | Footer links and brand social icons (`@icons-pack/react-simple-icons`). |
| `BusinessCard.tsx` | The summary card used on the home page, search results and related shops. Exports `OpenBadge` too, which **renders nothing when `open_state` is null** — null means hours unknown, and showing "Closed" for an open shop costs that business real money. Uses a plain `<img>` rather than `next/image` because the asset host is environment-dependent and `next/image` needs it declared at build time. |
| `icons.ts` | Runtime icon resolution. Exists only because `categories.category_icon` is a *string* in the database, so the component cannot be known at compile time. Everything else imports its icon directly. Unknown keys fall back to a default. |

### components/public/ — the browsing surface

| File | What it does |
| --- | --- |
| `AppShell.tsx` | Page chrome: header, side nav, content column, footer. A component rather than a layout **because layouts do not receive `searchParams`**, and the locale is `?lang=`. Also sets `lang=` and the Bengali font class on the content wrapper. |
| `Hero.tsx` | The home hero with the two-field search (what / where), also a GET form. Includes the hand-drawn SVG artwork, marked `aria-hidden` and hidden on small screens to leave room for the search field. |
| `CategoryGrid.tsx` | The category tiles. Holds the `TINTS` map with **full Tailwind class strings written out** — Tailwind scans source as text, so an interpolated `` `bg-${colour}-50` `` is never generated. |
| `FeaturedBusinesses.tsx` | Grid of featured shops, delegating each card to `BusinessCard`. |
| `HomeRail.tsx` | The three right-hand cards on the home page: `WhyShahebbazar` (static value proposition), `PopularSearches` (chips from the trends view), `UpcomingEvents` (date blocks). |
| `SideNav.tsx` | The left navigation rail plus a city promo card. Hidden below the `lg` breakpoint. Several of its links (categories, deals, events, blog) currently 404. |
| `SearchControls.tsx` | The filter bar. One GET form submitting category, area and sort together, with the search term carried as a hidden field so it survives a filter change. Also exports `searchHref()`, which merges current filters with changes to build a URL — used by the sidebar and the active-filter chips. Native `<select>` elements, for keyboard and mobile picker support. |
| `SearchResultRow.tsx` | A wider result row than the card — carries the description and address as well as rating and open state. |
| `SearchSidebar.tsx` | Area sketch map, facet filter lists with counts, and an "add your business" prompt. Facet counts come from the API and narrow as filters are applied. |
| `BusinessProfile.tsx` | The profile content column, 517 lines — the biggest component. Sections: overview/about, services, reviews with rating distribution, photos. Section navigation is **in-page anchor links, not tab panels**, so the whole profile is in the rendered markup for crawlers and no client component is needed. |
| `BusinessSidebar.tsx` | The profile sidebar: opening hours (with today highlighted, and "not set" when no rows exist), location sketch with a directions link, quick facts, and similar businesses. Holds its own `FACT_ICONS` map for `vendor_facts.fact_icon`. |

### public/

`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg` — untouched
`create-next-app` boilerplate. Nothing references them.
